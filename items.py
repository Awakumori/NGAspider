class Topic(object):
    def __init__(self, tid, title, poster_id, poster_name, post_time, re_num, sampling_time):
        self.tid = tid
        self.title = title
        self.poster_id = poster_id
        self.poster_name = poster_name
        self.post_time = post_time
        self.re_num = re_num
        self.sampling_time = sampling_time
